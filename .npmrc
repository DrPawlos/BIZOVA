# pnpm configuration
auto-install-peers=true

# Note: The "ignored build scripts" warning is informational only
# It doesn't affect your build or deployment
# sharp and unrs-resolver are safe dependencies used by Next.js
